config:
    agent_name: "billing"

deployment:
    llm:
        provider: "anthropic"
        model: "claude-haiku-4-5"
        api_key: "env(ANTHROPIC_API_KEY)"

start_agent main:
    description: "Billing agent."
    reasoning:
        instructions: ->
            | Hello.
