#!/usr/bin/env bash
# Run all buildpack tests.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BP_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

bash "$BP_DIR/test/detect.test.sh"
bash "$BP_DIR/test/compile.test.sh"
